{
  "name": "clinic-agency",
  "private": true,
  "scripts": {
    "convex:dev": "convex dev",
    "convex:check": "convex dev --once --typecheck=enable"
  },
  "dependencies": {
    "convex": "^1.27.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.8.0"
  }
}
